/** @format */

import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
	title: {
		fontSize: 24,
		fontWeight: "bold",
	},
	subtitle: {
		fontSize: 16,
		fontWeight: "bold",
		color: "black",
	},
	price: {
		fontSize: 16,
		color: "#333",
	},
});
